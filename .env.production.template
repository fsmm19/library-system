# ========================================
# Azure Production Environment Template
# ========================================
# This template contains all environment variables needed for Azure deployment
# DO NOT commit actual values - this is a template only
# Copy this file and fill in the actual values after Azure resources are created

# ========================================
# Database - Azure PostgreSQL Flexible Server
# ========================================
# Format: postgresql://username:password@server.postgres.database.azure.com:5432/database?ssl=true&sslmode=require
DATABASE_URL=postgresql://[DB_ADMIN_USERNAME]:[DB_ADMIN_PASSWORD]@[DB_SERVER_NAME].postgres.database.azure.com:5432/library?ssl=true&sslmode=require

# ========================================
# API Configuration - Azure App Service
# ========================================
NODE_ENV=production
PORT=8080
# Azure App Service uses port 8080 by default

# ========================================
# JWT Authentication
# ========================================
# Generate a secure random string for production
# Example: openssl rand -base64 32
JWT_SECRET=[GENERATE_SECURE_RANDOM_STRING]
JWT_EXPIRES_IN=7d

# ========================================
# Web Application - Azure Static Web Apps
# ========================================
# This will be the URL of the deployed API on Azure App Service
# Format: https://[API_APP_NAME].azurewebsites.net
NEXT_PUBLIC_API_URL=https://[API_APP_NAME].azurewebsites.net

# ========================================
# Azure Resource Names (for reference)
# ========================================
# These will be filled in after resource creation
# Resource Group: rg-library-system
# PostgreSQL Server: library-db-[unique-suffix]
# App Service Plan: asp-library-system
# API App Service: app-library-api-[unique-suffix]
# Static Web App: stapp-library-web

# ========================================
# CORS Configuration
# ========================================
# The API CORS settings will need to include the Static Web App URL
# Static Web App URL: https://[generated-url].azurestaticapps.net
# Or custom domain if configured
